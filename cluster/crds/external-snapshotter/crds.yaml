---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: external-snapshotter-crds
  namespace: flux-system
spec:
  interval: 30m
  url: https://github.com/kubernetes-csi/external-snapshotter.git
  ref:
    # renovate: datasource=github-releases repos=kubernetes-csi/external-snapshotter
    tag: v6.2.1
  ignore: |
    # exclude all
    /*
    # include crd directory
    !/client/config/crd
---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: external-snapshotter-crds
  namespace: flux-system
spec:
  interval: 30m
  prune: false
  sourceRef:
    kind: GitRepository
    name: external-snapshotter-crds
