---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: jackett
  namespace: media
spec:
  interval: 10m
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: jackett
      version: 6.4.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
  values:
    controllerType: deployment
    image:
      repository: k8sathome/jackett
      tag: v0.17.456
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: internal
        external-dns.alpha.kubernetes.io/target: milkyway.diaoul.fr
      hosts:
        - host: jackett.diaoul.fr
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - jackett.diaoul.fr
    persistence:
      config:
        enabled: true
        accessMode: ReadWriteOnce
        size: 1Gi
    podAnnotations:
      backup.velero.io/backup-volumes: config
    resources:
      requests:
        cpu: 500m
        memory: 250Mi
      limits:
        memory: 1500Mi
