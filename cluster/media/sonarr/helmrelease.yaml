---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sonarr
  namespace: media
spec:
  interval: 10m
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: sonarr
      version: 9.1.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
  values:
    image:
      repository: ghcr.io/k8s-at-home/sonarr
      tag: v3.0.4.1130
    ingress:
      enabled: true
      annotations:
        external-dns.alpha.kubernetes.io/target: milkyway.laniakea-sc.space
        cert-manager.io/cluster-issuer: letsencrypt-production
        nginx.ingress.kubernetes.io/auth-url: https://auth.milkyway.laniakea-sc.space/oauth2/auth
        nginx.ingress.kubernetes.io/auth-signin: https://auth.milkyway.laniakea-sc.space/oauth2/start
      hosts:
        - host: sonarr.milkyway.laniakea-sc.space
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - sonarr.milkyway.laniakea-sc.space
          secretName: sonarr-milkyway-laniakea-sc-space-tls
      additionalIngresses:
        - enabled: true
          nameSuffix: api
          annotations:
            external-dns.alpha.kubernetes.io/target: milkyway.laniakea-sc.space
          hosts:
            - host: sonarr.milkyway.laniakea-sc.space
              paths:
                - path: /api
                  pathType: Prefix
          tls:
            - hosts:
                - sonarr.milkyway.laniakea-sc.space
              secretName: sonarr-milkyway-laniakea-sc-space-tls
    persistence:
      config:
        enabled: true
        existingClaim: sonarr-config
    podAnnotations:
      backup.velero.io/backup-volumes: config
    additionalVolumeMounts:
      - name: nfs-singularity-downloads
        mountPath: /downloads
      - name: nfs-singularity-tvshows
        mountPath: /tvshows
    additionalVolumes:
      - name: nfs-singularity-downloads
        persistentVolumeClaim:
          claimName: nfs-singularity-downloads
      - name: nfs-singularity-tvshows
        persistentVolumeClaim:
          claimName: nfs-singularity-tvshows
    resources:
      requests:
        cpu: 500m
        memory: 250Mi
      limits:
        memory: 1500Mi
