---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sonarr
  namespace: media
spec:
  interval: 10m
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: sonarr
      version: 9.3.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
  values:
    image:
      repository: ghcr.io/k8s-at-home/sonarr
      tag: v3.0.5.1143
    ingress:
      enabled: true
      annotations:
        external-dns.alpha.kubernetes.io/target: diaoul.io
        cert-manager.io/cluster-issuer: letsencrypt-production
        nginx.ingress.kubernetes.io/auth-url: https://auth.diaoul.io/oauth2/auth
        nginx.ingress.kubernetes.io/auth-signin: https://auth.diaoul.io/oauth2/start
      hosts:
        - host: sonarr.diaoul.io
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - sonarr.diaoul.io
          secretName: sonarr-tls
      additionalIngresses:
        - enabled: true
          nameSuffix: api
          annotations:
            external-dns.alpha.kubernetes.io/target: diaoul.io
          hosts:
            - host: sonarr.diaoul.io
              paths:
                - path: /api
                  pathType: Prefix
          tls:
            - hosts:
                - sonarr.diaoul.io
              secretName: sonarr-tls
    persistence:
      config:
        enabled: true
        existingClaim: sonarr-config
    podAnnotations:
      backup.velero.io/backup-volumes: config
    additionalVolumeMounts:
      - name: nfs-singularity-downloads
        mountPath: /downloads
      - name: nfs-singularity-tvshows
        mountPath: /tvshows
    additionalVolumes:
      - name: nfs-singularity-downloads
        persistentVolumeClaim:
          claimName: nfs-singularity-downloads
      - name: nfs-singularity-tvshows
        persistentVolumeClaim:
          claimName: nfs-singularity-tvshows
    resources:
      requests:
        cpu: 100m
        memory: 600Mi
      limits:
        cpu: 200m
        memory: 600Mi
