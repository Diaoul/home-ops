---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: oauth2-proxy
  namespace: network-system
spec:
  interval: 10m
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: oauth2-proxy
      version: 4.4.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
  values:
    image:
      repository: quay.io/oauth2-proxy/oauth2-proxy
      tag: v6.1.1
    authenticatedEmailsFile:
      enabled: true
      restricted_access: |-
        ant.bertin@gmail.com
        maeva.gault@gmail.com
    ingress:
      enabled: true
      path: /oauth2
      annotations:
        external-dns.alpha.kubernetes.io/target: milkyway.laniakea-sc.space
        cert-manager.io/cluster-issuer: letsencrypt-production
      hosts:
        - auth.milkyway.laniakea-sc.space
      tls:
        - hosts:
            - auth.milkyway.laniakea-sc.space
          secretName: auth-milkyway-laniakea-sc-space-tls
    extraArgs:
      provider: oidc
      oidc-issuer-url: https://laniakea.eu.auth0.com/
      skip-provider-button:
      pass-authorization-header:
      email-domain: "*"
      cookie-domain: .milkyway.laniakea-sc.space
      whitelist-domain: .milkyway.laniakea-sc.space
  valuesFrom:
    - kind: Secret
      name: oauth2-proxy-helm-values
