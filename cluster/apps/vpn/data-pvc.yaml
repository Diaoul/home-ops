---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pod-gateway-data
  namespace: vpn
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: rook-ceph-block
