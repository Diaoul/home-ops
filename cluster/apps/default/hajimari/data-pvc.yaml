---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hajimari-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: rook-ceph-block
