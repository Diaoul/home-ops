---
kubernetes:
  children:
    master:
      hosts:
        k8s-node-1:
    worker:
      hosts:
        k8s-node-2:
        k8s-node-3:
        k8s-node-4:

unifi_controller:
  hosts:
    sun:

storage:
  hosts:
    singularity:

printer3d:
  hosts:
    curiosity:
