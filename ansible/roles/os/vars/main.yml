---
ansible_become: true
os_timezone: Europe/Paris
os_packages:
  install:
    - apt-transport-https
    - arptables
    - ebtables
    - nfs-common
    - open-iscsi
  remove:
    - apparmor
    - cloud-init
    - plymouth
    - snapd
    - ufw
