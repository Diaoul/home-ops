---
- name: unmount
  ansible.posix.mount:
    path: /var/lib/longhorn
    src: "{{ longhorn_device }}1"
    state: absent
  become: true

- name: delete mount directory
  ansible.builtin.file:
    path: /var/lib/longhorn
    state: absent
  become: true
