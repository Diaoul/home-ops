---
extends: default

ignore: |
  .yamllint.yml
  ansible/roles/calico/files/tigera-operator.yaml

rules:
  truthy:
    allowed-values: ['yes', 'no', 'true', 'false']
  indentation:
    spaces: 2
    indent-sequences: consistent
  line-length: disable
