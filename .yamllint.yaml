---
extends: default

ignore: |
  .github/
  user-data*.yaml
  *.sops.yml

rules:
  indentation:
    spaces: 2
    indent-sequences: consistent
  line-length: disable
  truthy:
    allowed-values: ['yes', 'no', 'true', 'false']
