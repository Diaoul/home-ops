---
extends: default

ignore: |
  .github/
  .direnv/
  ansible/roles/xanmanning.k3s/
  gotk-components.yaml
  user-data*.yaml
  *.sops.yml

rules:
  indentation:
    spaces: 2
  line-length: disable
  truthy:
    level: error
