---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: opencode-go-qwen3-6-plus
spec:
  # Served only on Zen's Anthropic-protocol endpoint; litellm appends
  # /v1/messages to apiBase, so it must stop at the plan root.
  modelName: opencode-go/qwen3.6-plus
  params:
    model: anthropic/qwen3.6-plus
    apiBase: https://opencode.ai/zen/go
    apiKeyRef:
      name: litellm
      key: OPENCODE_GO_API_KEY
    dropParams: true
  info:
    maxInputTokens: 262144
    maxOutputTokens: 32768
    supportsFunctionCalling: true
    supportsPromptCaching: true
    extra:
      supports_reasoning: true
